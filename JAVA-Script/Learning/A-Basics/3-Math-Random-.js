/*

let x = 5.31
let y = 2
let z;
 
 z = Math.random() #generate random random between 0 and 1

 z = Math.abs(x) #Make number Positive
 z = Math.sign(x) # z = 1 if x>0 |-1 if x<0 |0 if x=0

 z = Math.max(x,y,....) # z will have the max value
 z = Math.min(x,y,....) # z will have the min value

 z = Math.round(x) #round number
 z = Math.floor(x) #round number down
 z = Math.ceil(x) #round number up
 z = Math.trunc(x) #remove numbers after . in this case z =5

 PI = Math.PI
 e = Math.E = 2.71.....
 
 z = Math.pow(x,y)
 z = Math.sqrt(x)
 
 z = Math.log(x)

 z = Math.sin(x)
 z = Math.cos(x)
 z = Math.tan(x)

*/